# I recommend not going higher than 128, even 64 is quite heavy. 
hexcol_color_picker_map_size   (the color maps square dimensions)  int 64
hexcol_color_picker_mapUpdateTimeout (the minimum time between the color map update) float 2
hexcol_color_picker_item (add an item which can be held in the inventory) bool true
# Be aware that its not very compatible with other inventory mods
hexcol_color_picker_invmode (use the color picker as the main inventory) bool false