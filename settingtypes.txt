# I recommend not going higher than 128, even 64 is quite heavy. 
color_picker_map_size   (the color maps square dimensions)  int 64
color_picker_mapUpdateTimeout (the minimum time between the color map update) int 1